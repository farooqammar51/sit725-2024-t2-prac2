{{!-- <html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Calculator Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        html,
        body {
            height: 100%;
        }

        .wrapper {
            display: flex;
            flex-direction:
                column;
            min-height: 100%;
        }

        .content {
            flex: 1;
        }
    </style>
     <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
    />
</head>

<body>
    <div class="wrapper">
        <header class="bg-dark text-light py-4">
            <div class="container">
                <h1 class="text-center">Calculator</h1>
            </div>
        </header>

        <div class="content container mt-5">
            <div class="row">
                <div class="col-md-12">
                    <form action="/add" method="POST" class="row g-3">
                        <div class="col-md-3">
                            <label for="num1" class="form-label">Number 1:</label>
                            <input type="text" value="{{num1}}" id="num1" name="num1" class="form-control" required />
                        </div>
                        <div class="col-md-3">
                            <label for="num2" class="form-label">Number 2:</label>
                            <input type="text" value="{{num2}}" 
                            id="num2" name="num2" class="form-control" 
                            {{#if disableInput}}disabled{{/if}} required />
                        </div>
                        <div class="col-md-3">
                            <label for="num2" class="form-label">Result:</label>
                            <input disabled type="text" value="{{result}}" id="res" name="res" class="form-control" />
                        </div>
                </div>
            </div><br>
            <div class="row">
                <div class="col-md-3">
                    <div class="btn-group" role="group" aria-label="Basic example">
                        <button class="btn btn-outline-primary" 
                        type="submit">Add</button>
                        &nbsp;
                        <button class="btn btn-outline-danger" 
                        type="submit" formaction="/subtract">Subtract</button>&nbsp;
                        <button class="btn btn-outline-info" 
                        type="submit" formaction="/multiply">Multiply</button>&nbsp;
                        <button class="btn btn-outline-secondary" 
                        type="submit" formaction="/divide">Divide</button>&nbsp;
                    </div>
                </div>
            </div><br>
                <div class="row">
                <div class="col-md-3">
                    <div class="btn-group" role="group" aria-label="Basic example">
                        <button class="btn btn-outline-primary" 
                        type="submit" formaction="/exponentiate">Exponentiate</button>&nbsp;
                    </div>
                </div>
            </div><br>
            <div class="row">
                <div class="col-md-6">
                    <button class="btn btn-outline-warning" 
                    type="submit" formaction="/reset" class="btn btn-primary">Reset</button>
                </div> 
            </div>
            </form>
            <div class="text-danger text-center">
                {{#if error}}
                <p>Error: {{error}}</p>
                {{/if}}
            </div>
            <div class="text-info text-center">
                {{#if info}}
                <p>Additional information: {{info}}</p>
                {{/if}}
            </div>
        </div>

        <footer class="bg-dark text-light py-2">
            <div class="container text-center">
                <p>&copy; 2024 Calculator App</p>
            </div>
        </footer>
    </div>
</body>

</html> --}}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Calculator Form</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" rel="stylesheet" />
    <style>
        html,
        body {
            height: 100%;
        }

        .wrapper {
            display: flex;
            flex-direction: column;
            min-height: 100%;
        }

        .content {
            flex: 1;
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <header class="teal darken-2 white-text">
            <div class="container">
                <h1 class="center-align">Calculator</h1>
            </div>
        </header>

        <div class="content container">
            <div class="row">
                <form action="/add" method="POST" class="col s12">
                    <div class="row">
                        <div class="input-field col s4">
                            <label for="num1">Number 1:</label>
                            <input type="text" value="{{num1}}" id="num1" name="num1" required />
                        </div>
                        <div class="input-field col s4">
                            <label for="num2">Number 2:</label>
                            <input type="text" value="{{num2}}" id="num2" name="num2" 
                            {{#if disableInput}}disabled{{/if}} required />
                        </div>
                        <div class="input-field col s4">
                            <label for="res">Result:</label>
                            <input disabled type="text" value="{{result}}" id="res" name="res" />
                        </div>
                    </div>
                    <div class="row">
                        <div class="col s12">
                            <div class="btn-group">
                                <button class="btn waves-effect waves-light teal lighten-1" type="submit">Add</button>
                                <button class="btn waves-effect waves-light red lighten-1" type="submit" formaction="/subtract">Subtract</button>
                                <button class="btn waves-effect waves-light blue lighten-1" type="submit" formaction="/multiply">Multiply</button>
                                <button class="btn waves-effect waves-light grey lighten-1" type="submit" formaction="/divide">Divide</button>
                                <button class="btn waves-effect waves-light orange lighten-1" type="submit" formaction="/exponentiate">Exponentiate</button>
                                <button class="btn waves-effect waves-light yellow darken-2" type="submit" formaction="/reset">Reset</button>
                            </div>
                        </div>
                    </div>
                </form>
                <div class="col s12">
                    {{#if error}}
                    <p class="red-text center-align">Error: {{error}}</p>
                    {{/if}}
                    {{#if info}}
                    <p class="blue-text center-align">Additional information: {{info}}</p>
                    {{/if}}
                </div>
            </div>
        </div>

        <footer class="teal darken-2 white-text center-align">
            <div class="container">
                <p>&copy; 2024 Calculator App</p>
            </div>
        </footer>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>

</html>
